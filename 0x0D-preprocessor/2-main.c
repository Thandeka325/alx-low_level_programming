#include <stdio.h>

/**
 * main - Prints the name of the file i was compiled from
 * Followed by a new line
 * Alowed to use the stndard library
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("%s\n", __FILE__)
		return (0);
}
